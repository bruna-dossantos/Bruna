# Heads-up before installing this skill

**This is a draft/source copy — it does not run as-is.** Its internal name (in `SKILL.md`)
is **`criteria-writer`**, which is the SAME name as the criteria-writer skill you already have
**installed** (via the criteria-toolkit / anthropic-skills plugins).

- **As a draft, that's fine** — drafts sitting here don't run, so nothing clashes today.
- **If you ever install/package this one, rename it first** (e.g. `criteria-writer-headless`),
  or it will collide with the live `criteria-writer` and it'll be ambiguous which one runs.

What makes this one different: it's the **headless** variant — no questions, no screenshots,
no docx; it researches, drafts, cites, and submits via the `emit_criteria` tool.

_Moved here from ~/Downloads on 2026-08-13 (was a generic "criteria-writer" download folder)._
