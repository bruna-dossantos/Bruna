# Heads-up before installing this skill

**This is a draft/source copy — it does not run as-is.** Its internal name (in `SKILL.md`)
is **`criteria-writer-imaging`**. It's a sibling of the `criteria-writer` family, so it's close
in name/purpose to the criteria-writer skill you already have **installed**.

- **As a draft, that's fine** — drafts sitting here don't run, so nothing clashes today.
- **If you ever install/package this one, give it a clearly distinct name** so it doesn't get
  confused with the general `criteria-writer`.

What makes this one different: it's the **headless imaging** variant — covers MRI, MRA, CT, CTA,
PET, ultrasound, x-ray and related imaging codes; no questions/screenshots/docx, submits via the
`emit_criteria` tool.

_Moved here from ~/Downloads on 2026-08-13 (was the mis-named "files" folder; an identical
"files 2" duplicate was quarantined)._
